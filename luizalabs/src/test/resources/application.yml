security:
  jwt:
    token:
      secret-key: luizalabs
      expire-length: 3600000
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/favorite-products
    username: postgres
    password: luizalabs-interview
  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false